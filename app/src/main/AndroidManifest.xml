<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.afodevelop.chronoschedule">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".controllers.activities.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controllers.activities.MainActivity"
            android:label="@string/title_activity_Main"/>

        <activity
            android:name=".controllers.activities.SettingsActivity"
            android:label="@string/title_activity_Settings"/>

        <activity
            android:name=".controllers.activities.UserFormActivity"
            android:label="@string/title_activity_UserForm"/>

        <activity
            android:name=".controllers.activities.ShiftFormActivity"
            android:label="@string/title_activity_ShiftForm"/>

        <activity
            android:name=".controllers.activities.DayFormActivity"
            android:label="@string/title_activity_DayForm">

        </activity>

    </application>

</manifest>
